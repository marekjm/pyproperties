Release 0.1.2

    ->  pyproperties.py
        *   upd:    join() - changed order of argumets,
        *   upd:    merge() - added 'with_prefix' argumet,
                    read DOC for more information,
        *   upd:    melt() can now accept 'prefix' argument,
        *   upd:    save() saves both source and properties,
        *   new:    srcorigin variable for securing source,
                    (in 0.1.1 only properties were secure)

    Yours,
    Marek Marecki.